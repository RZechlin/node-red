// import got from 'got'

module.exports = function (RED) {
    "use strict";
    // Imports
    const got = require("got");

    function SiLACommand(n) {
        this.on("input",function (msg, nodeSend, nodeDone) {
            node.status({fill:"blue",shape:"dot",text:"SiLA requesting"});

            let url = 'http://localhost:80/'
            // Send
            got(url, )
        })

    }

    function SiLAProperty(n) {

    }
}